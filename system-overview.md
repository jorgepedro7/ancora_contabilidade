# Sistema Âncora Contabilidade – estado atual (06/04/2026)\n+\n+## Visão geral\n+- O backend Django 4.2 (`backend/config`) expõe módulos completos de `empresas`, `cadastros`, `fiscal`, `financeiro`, `folha`, `contábil`, `obrigações` e `estoque`. Cada app segue o padrão `ModelBaseEmpresa` com multi-tenancy baseada em `Empresa`/`Usuario` e JWT customizado em `apps/core`.\n+- O frontend Vue/Vite consome essas APIs via `/api/*` e já possui telas prontas para cadastros, fiscal, folha, financeiro, contábil/relatórios (router em `frontend/src/router/index.js`) e um layout com cabeçalho/barra lateral.\n+- A stack roda em Docker Compose com serviços: `db` (Postgres 15 + volume), `redis`, `backend` (Gunicorn), `frontend` (Vite dev server), `nginx` e workers Celery. O frontend proxya `/api` para `http://backend:8000` e o Compose injeta a variável `VITE_API_PROXY_TARGET` para garantir conexão dentro da rede Docker.\n+- O login usa `CustomTokenObtainPairSerializer` (ver `apps/core/serializers.py`) para entregar tokens com perfil de empresa e as permissões específicas.\n+- Sobre o plano de intake/portal do cliente, há um rascunho em `intake-plan.md` que lista modelos, APIs e workflow de exportação para o Questor.\n+\n+## Dados carregados hoje\n+- Empresas: 2 (ambas com CNPJ `123456780001xx` e nome “Âncora Contabilidade”).\n+- Usuários: 3 (um superuser administrativo criado com email `admin2@ancora.local`).\n+- Os uploads/documentos ainda seguem os modelos originais: `DocumentoFuncionario`, `NotaFiscal` e `MovimentacaoFinanceira` com `FileField` para PDF/XML, embora nenhum dado adicional tenha sido inserido automaticamente.\n+\n+## O que o sistema está fazendo agora\n+1. Autenticação JWT com refresh token e serializador customizado que insere campos extras no payload (`apps/core/serializers.py`).\n+2. Cada app registra dados estruturados: `empresas` para certificados/ambientes SEFAZ, `cadastros` para clientes/fornecedores/produtos, `fiscal` para NF-e (XML, DANFE, eventos), `folha` para contratos/pontos/documentos e `financeiro` para contas/lançamentos.\n+3. O frontend expõe dashboards e listas (ex.: `frontend/src/views/notaFiscalListView.vue`) que consomem esses endpoints e exibem status e relatórios.\n+4. O ambiente Docker mantém Postgres, Redis e workers sincronizados. Os serviços estão em `docker compose ps` com portas 8000 (backend), 3000 (frontend), 5433 (Postgres) e 6380 (Redis).\n+\n+## Documentação / passos para o portal do cliente (novo intake)\n+1. **Modelagem**: crie `apps/portal` (ou `apps/intake`) com modelos como `RecebimentoCliente`, `DocumentoRecebido`, `ChecklistCompetencia`, `PendenciaValidacao` e `LoteExportacaoQuestor`. Linke cada registro a `Empresa`, `Usuario` e, quando aplicável, a `NotaFiscal`, `Funcionario` ou `Lancamento`. Armazene `FileField`/hash/metadata e status (`novo`, `validado`, `reprovado`).\n+2. **APIs**: adicione viewsets/serializers REST em `apps/portal/views.py` com rotas `POST /api/portal/recebimentos/`, `GET /api/portal/pendencias/`, `POST /api/portal/exportar-questor/`. A autenticação já existe; reuso o middleware JWT ABC. Crie tarefas Celery (ex.: `portal.tasks.exportar_questor`) para montar ZIP/CSV e atualizar `LoteExportacaoQuestor`.\n+3. **Frontend**: estenda o router Vue com `/portal` e crie views para (a) formulário de upload com checklist e (b) painel de pendências com filtros e botões de exportação. Use os services (`frontend/src/services/api.js`) para chamar as APIs e exibir feedback. Guarde os uploads no storage (`MEDIA_ROOT` via Django). Aproveite os componentes de layout existentes.\n+4. **Processo externo**: defina um comando `python manage.py exportar_questor --lote=ID` que lê os documentos validados, gera o pacote (ex.: pasta com XML/planilha) e atualiza o status/importa log (veja `LogAtividade` em `apps/core/models.py`). Configure webhook/endpoint para disparar esse comando ao cliente confirmar recepção.\n+5. **Operações**: documente no `intake-plan.md` (já criado) as variáveis `.env` extras, os comandos de seed (ex.: `python manage.py seed_intake`), e os testes a rodar (`python manage.py test apps.portal`).\n+\n+Use este documento como referência antes de implementar o novo portal; o próximo passo seria definir os campos obrigatórios do intake e criar os modelos/serializers correspondentes.\n*** End Patch*** /home/jorge/Projetos/ancora_contabilidade/system-overview.md","workdir":"/home/jorge/Projetos","yield_time_ms":1000,"max_output_tokens":4000}**
